# Test fixture: a tiny stdio MCP server compiled to wasm32-wasip1 in CI.
# Detached from any parent workspace on purpose.
[package]
name = "toy-server"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde_json = "1"

[workspace]
